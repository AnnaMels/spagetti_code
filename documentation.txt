Задача:
Напишіть код, що генерує абсолютно випадкове число від 1 до 100

Щоб запустити код потрібно створити функцію, яка повертає результат і вивести її в консоль, щоб побачити результат:
function generateRandomNumber () {

   return ((((((Date.now() / (10**3 * parseInt('003C', 16) * parseInt('003C', 16) * 24)) / (0o30) / parseInt('003C', 16) / 10**3) % 'd'.charCodeAt(0))+'').slice(-2)) * 1)
}

console.log(generateRandomNumber())


